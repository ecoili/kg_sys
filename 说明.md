## 关于登录功能
1. 使用手机号+密码+图形验证码登录(后面要添加user_phone字段，非空) 这样的话用户名可以重名 √
2. 使用JWT认证 √
3. 登录时使用图形验证码或调用极验验证的滑块验证，~~倾向于滑块~~
   **geektest需要站点网址，需要上线项目，遂放弃，选择生成图形验证码**
4. 提供忘记密码功能(后期添加)

# vue 3.3.4  vue cli : 5.0.8
- 在frontend文件夹下使用``安装vue脚手架，做到**项目级隔离**
- 之后执行`npx vue create .`在当前目录即frontend中创建vue项目
- 注意只能使用**npx vue**作为命令前缀(?)，或配置**npm scripts**
- node_modules体积过大(目前项目总大小355MB，modules 255MB)   后期清理一下
  - [解决参考](https://blog.csdn.net/ShawBurton/article/details/130237814)----**不管用**
- pinia是vue3的状态管理，vuex是vue2的！
# flask 
## 启动项目
前置：用管理员权限打开cmd，输入`net start mysql`启动mysql
1. 前后端分别启动
   - 前端：在xxx/frontend路径下打开cmd命令行，输入`npm run serve`启动前端服务，运行在8081端口
     - vue cli启动用`npm run serve`,vite启动用`npm run dev`
   - 后端：在pycharm中直接点击运行，运行在5000端口
2. 前后端分别启动
   - 前端：在pycharm中输入`npm run serve`启动前端服务，运行在8081端口
   - 后端：进入flask3文件夹，打开cmd命令窗口，输入`.\venv\Scripts\activate`激活**虚拟环境**， 输入 `flask --app backend.app run`启动服务，运行在5000端口。
**注意 在flask3文件下执行`echo "FLASK_APP=backend.app" > .flaskenv`生成环境配置文件，指定app的位置，所以之后启动服务只需输入`flask run`即可**
   - 在外部命令行运行后端项目需要激活虚拟环境，在编译器里就不需要

*还需要`pip install python-dotenv`,且生成的.flaskenv需要另存为utf-8编码*

**Windows文件层级关系用反斜杠\!!!，用cd指令可以识别斜杠/，但涉及执行脚本时就需要反斜杠'\'**
**执行`rm -rf node_modules package-lock.json`Linux/unix风格命令可以在git bash执行，进入frontend文件夹** 

## 主页面问题
1. 首页是登录页面----当记住登录状态时直接显示登陆后的功能页？

## api
1. 请求路径自动添加api，转到5000端口，重写去掉api
2. 主页面 http:localhost:8081/ --->重定向到 /auth/login
3. 登录页面 /auth/login
4. 注册页面 /auth/register
5. 请求验证码路径 /auth/captcha
6. 以上属于认证登录模块，配置auth_bp，设置了url /auth
7. 

## 知识图谱
1. 初始化知识图谱相关函数
   1. 创建阵位节点
   2. 创建飞机节点
   3. 创建任务节点
   4. 创建阵位之间的联系  connected to
   5. 创建飞机与任务,阵位之间的联系 
2. 算法相关函数
   1. 寻找路径---按距离寻找
   2. 计算移动时间
   3. 模拟特情
   4. 重新分配任务





## migrate使用心得
1. 在app.py父文件夹下输入命令
2. 初次使用 `flask db init` 生成migrations文件
3. 当数据库表结构改变时 `flask db migrate -m "说明信息"` 生成迁移文件，此时还未提交到数据库。
4. `flask db upgrade` 提交修改到数据库，有错误的话会提交失败，按迁移文件的downgrade()方法回滚
成功的话按upgrade()方法提交。不用自己写，会自动生成
5. 版本控制类似git head指针指向最新提交版本 可回退到任意版本
6. 数据库中会生成一个表alembic_version,存储最新版本号

## 语法示例
1. 下划线
   - <u>eruhibve</u>
   - $\underline{\text{下划线}}$
2. 上划线
   * $\overline{\text{上划线}}$
3. 中划线
   + ~~发v你付了款大V~~
### 包管理
- requirements.txt
  - 保存整个环境下的包-> `pip freeze > requirements.txt`
  - 保存单个项目的包（可能一个环境下有多个项目）->`pip install pipreqs `， `pipreqs 当前路径`
  - 安装依赖：`pip install -r requirements.txt`
- 配置镜像源
  - npm前端项目：`npm config set registry https://registry.npmmirror.com`
    - 验证是否生效：`npm config get registry` 应输出：https://registry.npmmirror.com/ 
    - 恢复默认镜像源：`npm config set registry https://registry.npmjs.org`
  - pip后端项目：`pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple`
    - 验证是否生效： `pip config list`    应输出： global.index-url='https://pypi.tuna.tsinghua.edu.cn/simple'
    - 恢复默认镜像源：`pip config unset global.index-url`
问题
- token刷新函数中响应数据没有access_token属性
- 后端应该不需要传递用户信息（比较敏感），那前端就不用保存？
- 移植vue-element-admin作为后台dashboard面板。该项目使用vue2和element ui，需要迁移为vue3和element plus
- 一直不成功的话就自己写模版，使用element plus组件

学习
- apifox接口测试
- pinia持久化
- 验证码生成与存储
- 导航式（参数式，，？）
- 找一个适合的dashboard套用，很多dashboard都带很多模块，相当于后台管理




