1. 导入的库——关于深度学习，数据处理，可视化和评估的库

2. 运行模式——训练，单阵位预测，批量预测。预测参数（阵位ID，故障类型，严重程度，持续时间）、模型训练参数

3. 模型

   1. RGCN —— Relational graph convolutional network 关系图卷积网络

   2. GAT——Graph attention networks 图注意力网络

   3. #### `RGCN_GAT_Layer`——结合了关系图卷积网络（RGCN）和图注意力网络（GAT），用于提取图数据的特征。

   4. Transform模型，对输入序列进行特征提取

   5. #### `RGCN_GAT_Transformer`——将上述模块组合在一起，构建最终的多任务学习模型。输出层修改为同时输出分类概率和回归时间

4. 数据处理与批次准备

5. 训练函数

   1. set_seed 设置随机种子，保证实验可重复性
   2. train_model——训练多任务学习模型，包括**分类和回归任务**。计算类别权重，定义分类和回归损失函数，使用早停策略避免过拟合。在训练过程中，记录训练和验证损失、分类指标（AUC、F1 等）和回归指标（MAE），并绘制训练曲线。

6. 预测函数

   1. load-model——加载已训练的最佳多任务模型，并打印模型的训练配置和最佳验证指标。
   2. predict_single_position——**预测单个阵位**发生指定事件时的影响概率和影响时间，返回预测结果列表。
   3. visuallize_impact——可视化特情影响预测结果，将受影响和未受影响的阵位在图中标记出来，并标注影响概率和预测时间。
   4. batch_prediction——**批量预测所有阵位**发生指定事件的影响，保存统计信息和详细预测结果，并返回影响最广泛的前 10 个源阵位。

7. 主函数——根据用户配置的运行模式，选择执行训练、单阵位预测或批量预测任务，并处理可能出现的异常。

# 多任务学习模型

多任务学习模型是一种在**单个模型架构中同时学习和执行多个相关任务的机器学习模型**。这种模型的设计理念基于**多个任务之间存在共享信息或相关性**，通过同时学习这些任务，模型能够更有效地捕捉数据中的*共性特征*，提升在各个任务上的性能表现。

1. 模型结构与原理

   `RGCN_GAT_Transformer`类构建了多任务学习模型。它结合图神经网络（如`RGCN_GAT_Layer`）和 Transformer 模型，**先对节点特征进行处理**，**通过`RGCN_GAT_Layer`提取图结构特征，再将节点特征与事件相关特征组合，经 Transformer 模型进一步提取序列特征。**最后，通过修改输出层为`nn.Sequential(nn.Linear(self.transformer_input_dim, hidden_dim), nn.ReLU(), nn.Dropout(dropout), nn.Linear(hidden_dim, 2))`，使其可以同时输出两个值：一个用于分类（影响概率的 logit），一个用于回归（预测影响时间） 。这样，模型能同时学习**分类和回归两个任务**，共享中间层的特征表示。

2. 损失函数设计

   多任务学习模型通过调整不同任务损失函数的权重来平衡各个任务的学习。在代码中，使用`CLASSIFICATION_LOSS_WEIGHT`和`REGRESSION_LOSS_WEIGHT`分别设置分类任务和回归任务的损失权重。分类损失使用`nn.BCEWithLogitsLoss`，回归损失可选择`nn.MSELoss`（均方误差损失）或`nn.L1Loss`（平均绝对误差损失） ，并通过掩码操作仅计算受影响节点的回归损失，最后将两个损失按权重相加得到总损失：`total_loss = (config['classification_loss_weight'] * loss_cls + config['regression_loss_weight'] * loss_reg)`。这种方式确保模型在训练时能兼顾不同任务的学习需求。

3. 优势

   - **提高泛化能力**：通过共享特征学习，模型能更好地捕捉数据中的潜在规律，增强对不同任务的泛化能力。在阵位影响预测中，同时学习影响概率和影响时间，能使模型从不同角度理解数据，提高在新数据上的预测准确性。
   - **数据利用更高效**：多个任务共享数据和模型参数，相比单独训练多个模型，能更充分地利用有限的数据，减少过拟合风险。特别是在数据量不足时，多任务学习模型能借助相关任务的数据信息提升性能。
   - **学习任务间关系**：模型在学习过程中可以自动发现任务之间的相关性，从而优化特征提取和学习过程。在该代码场景下，影响概率和影响时间这两个任务相关，模型能学习到它们之间的内在联系，提升整体预测效果。

   

   ## 阵位影响预测和调度

   包含了数据加载与预处理、模型预测、任务调度算法等功能。

   1. genetic_scheduler_service.py

   该文件实现了**基于遗传算法的任务调度服务**，核心功能是根据阵位影响预测结果，为受影响的任务重新分配可用阵位。

   - 类和方法

     - ```
       GeneticSchedulerService
       ```

       - `__init__`：初始化预测服务和遗传算法配置。
       - `schedule_tasks`：根据预测结果，获取受影响阵位、需要调度的任务和可用阵位，运行遗传算法生成调度方案。
       - `_merge_schedules`：合并多个调度方案，解决资源冲突。
       - `_setup_genetic_algorithm`：初始化遗传算法的配置，包括创建适应度和个体类，注册操作算子。
       - `_get_affected_positions`：从预测结果中筛选出影响概率大于 10% 且预测影响时间大于 0 的阵位。
       - `_fetch_tasks_to_schedule`：查询受影响阵位上状态为 “待分配” 或 “已分配” 的任务。
       - `_fetch_available_positions`：查询所有未受影响的可用阵位。
       - `_run_genetic_algorithm`：运行遗传算法，寻找最优的任务分配方案。
       - `_evaluate_fitness`：评估个体的适应度，综合考虑任务优先级、紧迫性和距离等因素。
       - `_calculate_urgency_score`：计算任务的紧迫性得分。
       - `_calculate_distance_score`：计算任务与可用阵位之间的距离得分。
       - `_calculate_distance`：计算两个阵位之间的距离。
       - `_estimate_move_time`：预估任务从原阵位移动到新阵位所需的时间。
       - `_generate_schedule_plan`：根据遗传算法的最优个体，生成具体的调度方案。
       - `_get_position_by_id`：根据阵位 ID 获取阵位节点。
       - `_update_task_assignment`：更新 Neo4j 中任务的分配关系。
       - `_create_schedule_entry`：创建调度方案的条目，包含任务和阵位的详细信息。

   ### 2. base_prediction_service.py

   该文件定义了一个基础的预测服务类，负责加载预测模型和相关数据。

   - 类和方法

     ：

     - ```
       BasePredictionService
       ```

       ：

       - `__init__`：初始化模型路径、数据文件路径，加载设备，调用`load_prediction_model`函数加载模型和数据，并将模型设置为评估模式。

   ### 3. coordinator_service.py

   该文件实现了一个协调服务类，负责调用预测服务和调度服务，生成预测结果和调度方案。

   - 类和方法

     ：

     - ```
       CoordinatorService
       ```

       ：

       - `__init__`：初始化预测服务、调度服务，以及全局记录已调度任务和已占用阵位的集合。
       - `predict_and_schedule`：根据单个事件的信息，调用预测服务进行影响预测，再调用调度服务生成调度方案。
       - `batch_predict_and_schedule`：根据多个事件的信息，批量调用预测服务进行影响预测，为每个事件生成调度方案。

   ### 4. impact_prediction_service.py

   该文件提供了事件影响预测的服务，支持**单个事件和多个事件的批量预测**。它**接收事件的相关信息**，如事件发生的位置、事件类型、严重程度和持续时间等，然后**使用预定义的图神经网络模型进行预测，并对预测结果进行处理和格式化**。

   







