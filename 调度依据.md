这些代码文件主要围绕着阵位影响预测和任务调度展开，包含了数据加载与预处理、模型预测、任务调度算法等功能。下面是对每个文件的详细介绍：

### 1. genetic_scheduler_service.py

该文件实现了基于遗传算法的任务调度服务，核心功能是根据阵位影响预测结果，为受影响的任务重新分配可用阵位。

- 类和方法

  ：

  - ```
    GeneticSchedulerService
    ```

    ：

    - `__init__`：初始化预测服务和遗传算法配置。
    - `schedule_tasks`：根据预测结果，获取受影响阵位、需要调度的任务和可用阵位，运行遗传算法生成调度方案。
    - `_merge_schedules`：合并多个调度方案，解决资源冲突。
    - `_setup_genetic_algorithm`：初始化遗传算法的配置，包括创建适应度和个体类，注册操作算子。
    - `_get_affected_positions`：从预测结果中筛选出影响概率大于 10% 且预测影响时间大于 0 的阵位。
    - `_fetch_tasks_to_schedule`：查询受影响阵位上状态为 “待分配” 或 “已分配” 的任务。
    - `_fetch_available_positions`：查询所有未受影响的可用阵位。
    - `_run_genetic_algorithm`：运行遗传算法，寻找最优的任务分配方案。
    - `_evaluate_fitness`：评估个体的适应度，综合考虑任务优先级、紧迫性和距离等因素。
    - `_calculate_urgency_score`：计算任务的紧迫性得分。
    - `_calculate_distance_score`：计算任务与可用阵位之间的距离得分。
    - `_calculate_distance`：计算两个阵位之间的距离。
    - `_estimate_move_time`：预估任务从原阵位移动到新阵位所需的时间。
    - `_generate_schedule_plan`：根据遗传算法的最优个体，生成具体的调度方案。
    - `_get_position_by_id`：根据阵位 ID 获取阵位节点。
    - `_update_task_assignment`：更新 Neo4j 中任务的分配关系。
    - `_create_schedule_entry`：创建调度方案的条目，包含任务和阵位的详细信息。

### 2. base_prediction_service.py

该文件定义了一个基础的预测服务类，负责加载预测模型和相关数据。

- 类和方法

  ：

  - ```
    BasePredictionService
    ```

    ：

    - `__init__`：初始化模型路径、数据文件路径，加载设备，调用`load_prediction_model`函数加载模型和数据，并将模型设置为评估模式。

### 3. coordinator_service.py

该文件实现了一个协调服务类，负责调用预测服务和调度服务，生成预测结果和调度方案。

- 类和方法

  ：

  - ```
    CoordinatorService
    ```

    ：

    - `__init__`：初始化预测服务、调度服务，以及全局记录已调度任务和已占用阵位的集合。
    - `predict_and_schedule`：根据单个事件的信息，调用预测服务进行影响预测，再调用调度服务生成调度方案。
    - `batch_predict_and_schedule`：根据多个事件的信息，批量调用预测服务进行影响预测，为每个事件生成调度方案。

### 4. station_impact_prediction_multitask.py

该文件实现了阵位影响的多任务预测功能，包括数据加载与预处理、模型训练和预测等。

- 函数

  ：

  - `prepare_batch`：准备批次数据，包括源节点、事件类型、严重程度、持续时间、标签和真实时间张量。
  - `PositionalEncoding`：实现位置编码，用于在序列模型中加入位置信息。
  - `TimeEncoding`：实现时间编码，目前未在正向传播中使用，保留用于未来可能的扩展。
  - `load_and_preprocess_data`：加载并预处理阵位、关系、事件和影响数据，包括节点特征、边特征、事件类型编码和训练数据的生成。
  - `main`：主函数，根据运行模式（训练、预测、批量预测）执行相应的操作，包括数据加载、模型训练、预测和结果可视化。

这些文件共同构成了一个完整的阵位影响预测和任务调度系统，通过数据处理、模型预测和遗传算法优化，实现了对受影响任务的合理调度。

`impact_prediction_service.py` 文件定义了一个名为 `ImpactPredictionService` 的类，该类继承自 `BasePredictionService`，主要用于执行事件影响预测的相关任务。以下是对该文件具体作用的详细分析：

### 主要功能概述

该文件提供了事件影响预测的服务，支持单个事件和多个事件的批量预测。它接收事件的相关信息，如事件发生的位置、事件类型、严重程度和持续时间等，然后使用预定义的图神经网络模型进行预测，并对预测结果进行处理和格式化。

### 具体功能模块

#### 1. 单个事件影响预测

python



运行









```python
def predict_impact(self, source_position_id, event_type, severity, duration):
```

- **功能**：预测单个事件的影响。

- 步骤

  ：

  1. 验证输入的 `source_position_id` 和 `event_type` 是否有效。
  2. 将输入数据转换为 PyTorch 张量。
  3. 使用模型进行预测。
  4. 处理预测结果并返回。

#### 2. 多个事件批量影响预测

python



运行









```python
def predict_multitask_impact(self, source_position_ids, event_types, severities, durations):
```

- **功能**：批量预测多个事件的影响。

- 步骤

  ：

  1. 验证所有输入列表的长度是否一致。
  2. 遍历输入列表，验证每个 `source_position_id` 和 `event_type` 是否有效，并将其转换为节点索引。
  3. 将输入数据转换为 PyTorch 张量。
  4. 使用模型进行批量预测。
  5. 处理批量预测结果并返回。

#### 3. 单个预测结果处理

python



运行









```python
def _process_prediction_results(self, outputs):
```

- **功能**：处理单个预测结果，将模型输出转换为易于理解的字典列表。

- 步骤

  ：

  1. 提取影响概率和影响时间。
  2. 计算影响概率的 sigmoid 值。
  3. 遍历每个节点，构建预测结果字典。

#### 4. 批量预测结果处理

python



运行









```python
def _process_batch_prediction_results(self, outputs, source_ids, event_types):
```

- **功能**：处理批量预测结果，将模型输出转换为嵌套的字典列表。

- 步骤

  ：

  1. 提取影响概率和影响时间。
  2. 计算影响概率的 sigmoid 值。
  3. 遍历每个批次和每个节点，构建预测结果字典。

#### 5. 预测错误处理

python



运行









```python
def _handle_prediction_error(self, error):
```

- **功能**：统一处理预测过程中发生的错误。

- 步骤

  ：

  1. 打印错误信息和堆栈跟踪。
  2. 抛出包含错误信息的 `ValueError` 异常。

### 总结

该文件提供了一个完整的事件影响预测服务，包括输入验证、模型预测、结果处理和错误处理等功能。它通过封装这些功能，使得调用者可以方便地进行单个或批量事件的影响预测。impact_prediction_service.py 文件定义了一个名为 `ImpactPredictionService` 的类，该类继承自 `BasePredictionService`，主要用于执行事件影响预测的相关任务。以下是对该文件具体作用的详细分析：

### 主要功能概述

该文件提供了事件影响预测的服务，支持单个事件和多个事件的批量预测。它接收事件的相关信息，如事件发生的位置、事件类型、严重程度和持续时间等，然后使用预定义的图神经网络模型进行预测，并对预测结果进行处理和格式化。

### 具体功能模块

#### 1. 单个事件影响预测

python



运行









```python
def predict_impact(self, source_position_id, event_type, severity, duration):
```

- **功能**：预测单个事件的影响。

- 步骤

  ：

  1. 验证输入的 `source_position_id` 和 `event_type` 是否有效。
  2. 将输入数据转换为 PyTorch 张量。
  3. 使用模型进行预测。
  4. 处理预测结果并返回。

#### 2. 多个事件批量影响预测

python



运行









```python
def predict_multitask_impact(self, source_position_ids, event_types, severities, durations):
```

- **功能**：批量预测多个事件的影响。

- 步骤

  ：

  1. 验证所有输入列表的长度是否一致。
  2. 遍历输入列表，验证每个 `source_position_id` 和 `event_type` 是否有效，并将其转换为节点索引。
  3. 将输入数据转换为 PyTorch 张量。
  4. 使用模型进行批量预测。
  5. 处理批量预测结果并返回。

#### 3. 单个预测结果处理

python



运行









```python
def _process_prediction_results(self, outputs):
```

- **功能**：处理单个预测结果，将模型输出转换为易于理解的字典列表。

- 步骤

  ：

  1. 提取影响概率和影响时间。
  2. 计算影响概率的 sigmoid 值。
  3. 遍历每个节点，构建预测结果字典。

#### 4. 批量预测结果处理

python



运行









```python
def _process_batch_prediction_results(self, outputs, source_ids, event_types):
```

- **功能**：处理批量预测结果，将模型输出转换为嵌套的字典列表。

- 步骤

  ：

  1. 提取影响概率和影响时间。
  2. 计算影响概率的 sigmoid 值。
  3. 遍历每个批次和每个节点，构建预测结果字典。

#### 5. 预测错误处理

```python
def _handle_prediction_error(self, error):
```

- **功能**：统一处理预测过程中发生的错误。
- 步骤
  1. 打印错误信息和堆栈跟踪。
  2. 抛出包含错误信息的 `ValueError` 异常。

### 总结

该文件提供了一个完整的事件影响预测服务，包括输入验证、模型预测、结果处理和错误处理等功能。它通过封装这些功能，使得调用者可以方便地进行单个或批量事件的影响预测。

# 随机种群规模为300

在遗传算法中选择随机种群规模为300（如代码中`pop_size = min(300, 15 * individual_length)`）是经过权衡计算效率和搜索能力后的经验性选择。以下是具体原因分析：

------

### 1. **平衡探索与计算成本**

- **探索能力**：较大的种群（如300）能维护更高的遗传多样性，避免早熟收敛，尤其在多模态问题中更可能找到全局最优解。
- **计算效率**：300是一个在普通硬件上可接受的上限，超过此值可能导致单代计算时间过长（适应度评估涉及任务调度成本计算）。

------

### 2. **动态调整机制**

代码中实际使用`min(300, 15 * individual_length)`，体现两种策略：

- 

  基于任务数量的缩放

  ：当任务较少时（

  ```
  individual_length
  ```

  小），按

  ```
  15 * individual_length
  ```

  计算，确保足够个体覆盖解空间。

  - 例如：5个任务 → 75个个体

- **上限控制**：当任务较多时（`individual_length ≥ 20`），限制为300避免资源浪费。

------

### 3. **文献支持**

- **经验法则**：经典遗传算法建议种群规模在50~500之间，300处于中间偏大位置，适合复杂调度问题。
- **任务相关性**：调度问题中，每个任务可能有多个可行解（阵位选择），种群需足够大以覆盖这些组合。

------

### 4. **算法参数协同**

其他参数（如`cxpb=0.8`, `mutpb=0.3`, `ngen=100`）与300的种群规模协同：

- **高交叉概率**（0.8）依赖大种群提供优质父代。
- **适度变异率**（0.3）在大种群中能有效维持多样性而不破坏优良基因。

------

### 5. **问题特性适配**

- **调度问题的复杂性**：任务分配涉及多目标（优先级、距离、资源匹配），大种群有助于探索非支配解。
- **约束处理**：300的规模提供更多机会生成满足约束（如阵位唯一性）的可行解。

------

### 对比其他可能选择

|  种群规模  |      优点      |     缺点     |        适用场景        |
| :--------: | :------------: | :----------: | :--------------------: |
| **50-100** |     计算快     |    易早熟    |   简单问题/实时需求    |
|  **300**   |    解质量高    | 适度计算成本 | 复杂调度（如当前代码） |
|  **500+**  | 全局最优概率高 |   计算昂贵   |        离线优化        |

### 两点交叉（Two-point Crossover）详解

#### 1. 基本操作原理

- **染色体分割**：随机选择两个交叉点（如基因序列的第3和第7位）

- **基因交换**：父代A和父代B在这两点之间的片段互换

- 

  示例

  ：

  markdown

  复制

  ```
  父代A: [1,2,|3,4,5,6,|7,8]  
  父代B: [a,b,|c,d,e,f,|g,h]
  子代A: [1,2,|c,d,e,f,|7,8]  
  子代B: [a,b,|3,4,5,6,|g,h]
  ```

#### 2. 在调度系统中的优势

- **保持结构完整性**：相比单点交叉，能更好保留任务序列的连续区块
- **探索能力增强**：80%的高概率促进优质调度方案的组合创新
- **典型应用场景**：适用于变长染色体或需要保护特定基因块的问题

#### 3. 参数设置建议

- 

  80%概率

  的合理性：

  - 平衡探索（交叉）与开发（保留）的需求
  - 配合15%变异率形成"强交叉弱变异"策略
  - 文献推荐范围：60%-90%（DEAP框架默认0.7）

### 位翻转变异（Bit Flip Mutation）详解

#### 1. 操作机制

- **随机选择**：以15%概率选中某基因位

- 

  数值翻转

  ：

  - 二进制编码：0↔1
  - 实数编码：在定义域内随机重置
  - 调度问题示例：任务分配位置随机变更

#### 2. 在您系统中的作用

- **维持多样性**：防止种群陷入局部最优
- **恢复丢失基因**：重新引入可能被淘汰的有用基因
- **微调能力**：适合离散型调度问题的解空间探索

#### 3. 概率设置分析

| 变异率  |   效果   |    风险    |
| :-----: | :------: | :--------: |
| 5%-10%  | 开发导向 |  早熟收敛  |
| **15%** | 平衡策略 |  适度扰动  |
|  20%+   | 强探索性 | 破坏优良解 |

### 参数协同效应

两项操作共同构成遗传算法的搜索引擎：

搜索能力=0.8×两点交叉+0.15×位翻转

- 

  每代预期变化

  ：

  - 约240个个体（300×80%）经历交叉
  - 约45个基因位（300×基因长度×15%）发生变异

### 实现示例（Python伪代码）

python

复制

```
def two_point_crossover(parent1, parent2):
    length = len(parent1)
    pt1, pt2 = sorted(random.sample(range(1, length), 2))
    child1 = parent1[:pt1] + parent2[pt1:pt2] + parent1[pt2:]
    child2 = parent2[:pt1] + parent1[pt1:pt2] + parent2[pt2:] 
    return child1, child2

def bit_flip_mutation(individual):
    for i in range(len(individual)):
        if random.random() < 0.15:
            individual[i] = random.choice(available_positions)
    return individual
```

### 工程实践建议

1. **两点交叉优化**：
   - 对连续型变量可改用SBX（模拟二进制交叉）
   - 对调度问题建议增加可行性检查（避免重复分配）
2. **变异增强**：
   - 对关键基因（如高优先级任务）可降低变异概率
   - 采用自适应变异率：随代数增加递减

这种参数组合特别适合您描述的阵位调度问题，能在保证方案可行性的同时有效探索解空间。